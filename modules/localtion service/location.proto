syntax = "proto3";

message LocationtMessage {
    int32 userId = 1;
    int32 latitude = 2;
    int32 longitude = 3;
  }

service Location_Service {
  rpc Create(LocationtMessage) returns (LocationtMessage);
}